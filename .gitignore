wiki/external/

# Ephemeral build staging — regenerated every CI build from external.yml +
# local-plugins via scripts/mirror.js + scripts/flatten-plugins.js. Never edit
# or commit content here; edit the sources instead (see agents.md directory map).
wiki/plugins/
wiki/themes/
wiki/tiddlers/
wiki/tiddlywiki.info
wiki/output/
wiki/mirror.log
wiki/flatten-manifest.log
wiki/tiddlywiki.log
/scratch/

# Local per-machine agent instructions (see .freebuff/local/agents.local.md)
.freebuff/

# Uglify plugin's on-disk compression cache (default: ./.cache/uglify)
/.cache/

# Local-only convenience exports for meatspace testing (regenerable via scripts/make-rollup.py)
/rollups/

# Launcher UI dependencies and build output
/launcher-ui/node_modules/
/launcher-ui/dist/
/launcher-ui/.vite/

# Tauri build output
/src-tauri/target/

# Generated single-file launcher intermediates.
# NOTE: /src/launcher.html is deliberately NOT ignored. It is generated, but it is
# also PUBLISHED: deploy/autoupdate.sh fetches it from raw.githubusercontent.com at
# runtime, and deploy/Dockerfile does `COPY src/launcher.html`. Ignoring it would
# break every deployed server's launcher update path and the Docker image build.
/src/index.html
/src/launcher.js
/src/launcher.css
/src/launcher-fragments-assembled.html

# Root dependency install
/node_modules/

# Scratch space for local/agent tooling and one-off harnesses: screenshots,
# generated comparison images, throwaway scripts. Never tracked, nothing in the
# repo reads it.
/tmp/

# Local preview and browser-test artifacts
/.launcher-preview.log
/scratch/test-profile-*/
/scratch/chrome-test-profile/
/scratch/test-saver-headless.html
