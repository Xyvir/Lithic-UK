name: Windows Manual Release

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      tag_name:
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Download Built Wiki Artifact
        if: ${{ inputs.tag_name != '' }}
        uses: actions/download-artifact@v5
        with:
          name: built-wiki
          path: src

      - name: Rename Downloaded Wiki
        if: ${{ inputs.tag_name != '' }}
        run: Move-Item -Force src/lith.html src/lithic.html

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22


      # Keyed on the lockfiles, not package.json: the pins are what decide which
      # toolchain emits the artifacts, so a pin change has to invalidate the cache.
      - name: Cache NPM dependencies
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json', 'launcher-ui/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: 'src-tauri -> target'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Dependencies
        run: |
          npm ci
          npm --prefix launcher-ui ci

      - name: Run Launcher UI Tests
        run: npm --prefix launcher-ui test

      - name: Build Launcher (Svelte Launcher Bundle)
        run: node scripts/build-launcher.mjs

      # --- DEBUG STEP ---
      - name: Debug - List Files & Tauri Info
        run: |
          Write-Host "--- CURRENT DIRECTORY ---"
          Get-Location
          Write-Host "`n--- FILE STRUCTURE (Depth 3) ---"
          Get-ChildItem -Path . -Recurse -Depth 3 | Select-Object FullName
          Write-Host "`n--- TAURI INFO ---"
          npm run tauri info

      - name: Generate Icons
        run: |
          if (Test-Path "src/app-icon.png") {
            npm run tauri icon src/app-icon.png
          } else {
            Write-Host "Icon file 'src/app-icon.png' not found, skipping icon generation."
          }

      - name: Generate Date-Based Tag
        id: tag
        run: |
           if ("${{ inputs.tag_name }}" -ne "") {
             $tag = "${{ inputs.tag_name }}"
           } else {
             $tag = "v$(Get-Date -Format 'yyyy.MM.dd-HHmm')"
           }
           echo "date=$tag" >> $env:GITHUB_OUTPUT
           # Semver for the exe's FileVersion/ProductVersion resources:
           # tauri-build derives them from package.version, which we patch
           # via TAURI_CONFIG at build time. Leading zeros are stripped
           # (semver forbids them) and a non-version tag falls back to the
           # config version so the build never fails.
           if ($tag -match '(\d+)\.(\d+)\.(\d+)') {
             $ver = "$($Matches[1]).$([int]$Matches[2]).$([int]$Matches[3])"
           } else {
             $ver = "0.2.0"
           }
           echo "version=$ver" >> $env:GITHUB_OUTPUT
           echo ('tauri_config={"package":{"version":"' + $ver + '"}}') >> $env:GITHUB_OUTPUT

      - name: Build Offline Executable
        run: npm run tauri build
        env:
          TAURI_CONFIG: ${{ steps.tag.outputs.tauri_config }}

      - name: Rename Offline Artifact
        run: |
          $exe = Get-ChildItem -Path "src-tauri/target/release" -Filter "*.exe" -Recurse | Where-Object { $_.FullName -notmatch "deps" -and $_.FullName -notmatch "build" } | Select-Object -First 1
          if ($exe) {
            Write-Host "Found EXE at: $($exe.FullName)"
            Copy-Item $exe.FullName -Destination "Lithic-Offline-${{ steps.tag.outputs.date }}.exe"
            Write-Host "Created Lithic-Offline-${{ steps.tag.outputs.date }}.exe"
          } else {
            Write-Error "Build failed: No .exe found in target/release folder."
            exit 1
          }

      # --- ONLINE BUILD DISABLED ---
      # - name: Switch Config to Online Mode
      #   run: |
      #     $configPath = "src-tauri/tauri.conf.json"
      #     $json = Get-Content $configPath -Raw | ConvertFrom-Json
      #
      #     # Change entry point to website
      #     if ($json.tauri -and $json.tauri.windows) {
      #        $json.tauri.windows[0].url = "https://lithic.uk"
      #     }
      #
      #     $json | ConvertTo-Json -Depth 10 | Set-Content $configPath

      # - name: Build Online Executable
      #   run: npm run tauri build

      # - name: Rename Online Artifact
      #   run: |
      #     $exe = Get-ChildItem -Path "src-tauri/target/release/*.exe" | Select-Object -First 1
      #     if ($exe) {
      #       Move-Item $exe.FullName -Destination "Lithic-Online.exe"
      #        Write-Host "Created Lithic-Online.exe"
      #     }
      # -----------------------------

      - name: Create Release
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.date }}
          draft: false
          prerelease: false
          files: |
            Lithic-Offline-${{ steps.tag.outputs.date }}.exe
            # Lithic-Online.exe
