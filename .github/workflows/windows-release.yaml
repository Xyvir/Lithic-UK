name: Windows Manual Release

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      tag_name:
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Download Built Wiki Artifact
        if: ${{ inputs.tag_name != '' }}
        uses: actions/download-artifact@v5
        with:
          name: built-wiki
          path: src

      - name: Rename Downloaded Wiki
        if: ${{ inputs.tag_name != '' }}
        run: Move-Item -Force src/lith.html src/lithic.html

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22


      - name: Cache NPM dependencies
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: 'src-tauri -> target'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Dependencies
        run: |
          npm install
          npm --prefix launcher-ui install --no-package-lock

      - name: Run Launcher UI Tests
        run: npm --prefix launcher-ui test

      - name: Build Pre-Launcher (Svelte Launcher Bundle)
        run: node scripts/build-pre-launcher.mjs

      # --- DEBUG STEP ---
      - name: Debug - List Files & Tauri Info
        run: |
          Write-Host "--- CURRENT DIRECTORY ---"
          Get-Location
          Write-Host "`n--- FILE STRUCTURE (Depth 3) ---"
          Get-ChildItem -Path . -Recurse -Depth 3 | Select-Object FullName
          Write-Host "`n--- TAURI INFO ---"
          npm run tauri info

      - name: Generate Icons
        run: |
          if (Test-Path "src/app-icon.png") {
            npm run tauri icon src/app-icon.png
          } else {
            Write-Host "Icon file 'src/app-icon.png' not found, skipping icon generation."
          }

      - name: Build Offline Executable
        run: npm run tauri build

      - name: Rename Offline Artifact
        run: |
          $exe = Get-ChildItem -Path "src-tauri/target/release" -Filter "*.exe" -Recurse | Where-Object { $_.FullName -notmatch "deps" -and $_.FullName -notmatch "build" } | Select-Object -First 1
          if ($exe) {
            Write-Host "Found EXE at: $($exe.FullName)"
            Copy-Item $exe.FullName -Destination "Lithic-Offline.exe"
            Write-Host "Created Lithic-Offline.exe"
          } else {
            Write-Error "Build failed: No .exe found in target/release folder."
            exit 1
          }

      # --- ONLINE BUILD DISABLED ---
      # - name: Switch Config to Online Mode
      #   run: |
      #     $configPath = "src-tauri/tauri.conf.json"
      #     $json = Get-Content $configPath -Raw | ConvertFrom-Json
      #
      #     # Change entry point to website
      #     if ($json.tauri -and $json.tauri.windows) {
      #        $json.tauri.windows[0].url = "https://lithic.uk"
      #     }
      #
      #     $json | ConvertTo-Json -Depth 10 | Set-Content $configPath

      # - name: Build Online Executable
      #   run: npm run tauri build

      # - name: Rename Online Artifact
      #   run: |
      #     $exe = Get-ChildItem -Path "src-tauri/target/release/*.exe" | Select-Object -First 1
      #     if ($exe) {
      #       Move-Item $exe.FullName -Destination "Lithic-Online.exe"
      #        Write-Host "Created Lithic-Online.exe"
      #     }
      # -----------------------------

      - name: Generate Date-Based Tag
        id: tag
        run: |
           if ("${{ inputs.tag_name }}" -ne "") {
             echo "date=${{ inputs.tag_name }}" >> $env:GITHUB_OUTPUT
           } else {
             echo "date=v$(Get-Date -Format 'yyyy.MM.dd-HHmm')" >> $env:GITHUB_OUTPUT
           }

      - name: Create Release
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.date }}
          draft: false
          prerelease: false
          files: |
            Lithic-Offline.exe
            # Lithic-Online.exe
